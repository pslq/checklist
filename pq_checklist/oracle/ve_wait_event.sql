select  inst_id, nvl(event,'cpu')event, WAIT_CLASS, count(* ) sample_count from gv$active_session_history where sample_time >= sysdate - interval 'PQ_SECONDS_MONITOR' second group by inst_id , event,wait_class order by 4
